# Local design mockups — not hosted, not part of the guide build
/mockups/

# Superpowers working docs (specs/plans) — kept local, not committed
/docs/superpowers/

/node_modules/
/dist/

# Wrangler local state and `deploy --dry-run` output (build:check).
/.wrangler/
